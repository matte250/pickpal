{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "run web",
        "group": "dev",
        "command": "bun --env-file=../.env.local run dev",
        "options": {
            "cwd": "${workspaceFolder}/web"
        }
      }
    ]
  }